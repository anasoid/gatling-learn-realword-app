package {{package}};

/**
 * Stable, public-facing entry point for {{classname}} operations.
 *
 * <p>Extends {@link Generated{{classname}}}, which holds all the actual generated request
 * methods (as normal instance methods) and is fully regenerated on every build. Reference this
 * class (not {@code Generated{{classname}}}) from simulations and tests, via {@link #instance()}.
 *
 * <p>Holds the single shared instance used by callers, e.g.
 * {@code Default{{classname}}.instance().login(...)}.
 */
public class Default{{classname}} extends Generated{{classname}} {

    private static Default{{classname}} instance = new Default{{classname}}();

    /**
     * Returns the shared instance callers should use to invoke {{classname}} operations.
     *
     */
    public static Default{{classname}} instance() {
        return instance;
    }


}
